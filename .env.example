# Copia este archivo como ".env" (sin ".example") y ajusta si hace falta.
# docker compose lo lee automáticamente.

# Base de datos
POSTGRES_USER=luzdeluna
POSTGRES_PASSWORD=cambia_esta_clave
POSTGRES_DB=luzdeluna
DATABASE_URL=postgresql://luzdeluna:cambia_esta_clave@postgres:5432/luzdeluna

# Backend
PORT=4000
JWT_SECRET=cambia_este_secreto_por_uno_largo_y_aleatorio
NODE_ENV=development

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:4000
